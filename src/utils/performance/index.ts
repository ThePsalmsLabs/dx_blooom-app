'use client'

import { useMemo } from 'react'

// Base performance metrics hook placeholder.
// Replace with your real implementation when available.
export function usePerformanceMetrics(): Record<string, unknown> {
  return useMemo(() => ({}), [])
}


